import React from 'react';

import UserDataTable from './UserDataTable';

const UserDashboard = () => {
  // const [vehicleData, setVehicleData, getVehicleData] = useContext(
  //   UserDataContext
  // );

  // useEffect(() => {
  //   getVehicleData();
  // }, []);

  return <UserDataTable />;
};

export default UserDashboard;
